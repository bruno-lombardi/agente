@layout("layouts/app")

@section("title")
    Registre-se no Agente
@endsection

@section("c-scripts")
    {{ script("/js/bday-picker.js") }}
    <script>
        $("#bday").birthdaypicker(options={dateFormat: "littleEndian", monthFormat: "long"});
    </script>
@endsection

@section("content")

@include("partials/navbar-guest")

<div class="unix-login">
    <div class="container-fluid">
        <div class="row justify-content-center">
            <div class="col-lg-4">
                <div class="login-content card">
                    <div class="login-form">
                        <h4>Registrar</h4>
                        <form method="POST" action="{{ route('storeUser') }}">
                            {{ csrfField() }}
                            <div class="form-row">
                                <div class="col-md-6 form-group">
                                    <label>Nome</label>
                                    <input type="text" class="form-control" placeholder="Neymar" name="name">
                                </div>
                                <div class="col-md-6 form-group">
                                    <label>Sobrenome</label>
                                    <input type="text" class="form-control" placeholder="Júnior" name="lastname">
                                </div>
                            </div>
                            <div id='bday'>
                                
                            </div>

                            <div class="form-group">
                                <label>Endereço de email</label>
                                <input type="email" class="form-control" placeholder="Informe um email válido" name="email">
                            </div>
                            <div class="form-group">
                                <label>Senha</label>
                                <input type="password" class="form-control" placeholder="Insira uma senha segura" name="password">
                            </div>
                            <div class="checkbox">
                                <label>
                                <input type="checkbox" name="terms"> Você leu e aceitou os <a href="">termos de uso e privacidade</a>?
                            </label>
                            </div>
                            <button type="submit" class="btn btn-primary btn-flat m-b-30 m-t-30">Registrar</button>
                            <div class="register-link m-t-15 text-center">
                                <p>Já possui uma conta ? <a href="/entrar"> Entrar</a></p>
                            </div>
                        </form>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

@endsection

