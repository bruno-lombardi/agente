<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <!-- Tell the browser to be responsive to screen width -->
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta name="description" content="">
    <meta name="author" content="">
    <!-- Favicon icon -->
    <link rel="icon" type="image/png" sizes="16x16" href="images/favicon.png">
    <title>Home</title>
    {{ css("/css/lib/toastr/toastr.min.css") }}
    <!-- Bootstrap Core CSS -->
    {{ css("/css/lib/bootstrap/bootstrap.min.css") }}
    <!-- Custom CSS -->
    {{ css("/css/helper.css") }}
    {{ css("/css/style.css") }}
    <!-- HTML5 Shim and Respond.js IE8 support of HTML5 elements and media queries -->
    <!-- WARNING: Respond.js doesn't work if you view the page via file:** -->
    <!--[if lt IE 9]>
    <script src="https:**oss.maxcdn.com/libs/html5shiv/3.7.0/html5shiv.js"></script>
    <script src="https:**oss.maxcdn.com/libs/respond.js/1.4.2/respond.min.js"></script>
<![endif]-->
</head>
<body>
    <div class="container">
        <div class="row justify-content-center">
            <div class="col-lg-6">
                <div class="card">
                    <div class="card-title"><h3>Editar tarefa</h3></div>
                    <div class="card-body">

                        <div class="basic-form">
                            <form method="POST" action="{{ route('updateTodo', {id: todo.id}) }}">
                                {{ csrfField() }}
                                <div class="form-group">
                                    <label>Nome</label>
                                    <input class="form-control" value="{{todo.title}}" type="text" name="title">
                                    @if(hasErrorFor("title"))
                                        <small class="animated fadeInDown text-danger">{{ getErrorFor("title")}}</small>
                                    @endif
                                </div>
                                
                                
                                <div class="form-group">
                                    <label>Descrição</label>
                                    <textarea style="height: auto" class="form-control" rows="3" name="content">{{ todo.content }}</textarea>
                                    @if(hasErrorFor("content"))
                                        <small class="animated fadeInDown text-danger">{{ getErrorFor("content")}}</small>
                                    @endif
                                </div>

                                <button type="submit" class="btn btn-primary">Salvar</button>
                            </form>
                        </div>
                                
                    </div>
                </div>
            </div>
        </div>
    </div>
</body>
</html>